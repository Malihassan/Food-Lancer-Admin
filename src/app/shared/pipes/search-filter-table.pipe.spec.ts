import { SearchFilterTablePipe } from './search-filter-table.pipe';

describe('SearchFilterTablePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterTablePipe();
    expect(pipe).toBeTruthy();
  });
});
